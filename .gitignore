*.retry
*.pyc
__pycache__/
.molecule/
.cache/
*.log
.DS_Store
.pytest_cache/
.docker/ssh-keys/
.docker/.env
workspace/.ssh/
venv/

# Range ground-truth — randomised IPs + per-host nonces. NEVER commit.
# (The cadet's own recon/ and reports/ artefacts ARE committed — that is the
# submission ARIA reviews.)
.lab/

# Internal dev files
CLAUDE.md
AGENTS.md
IMPLEMENTATION_PLAN.md
ideas.md
fixes.md
PLAN.md

# IDE
.idea/
.vscode/
*.swp

# Command Center
.cc-config.json

# Reference solution / build E2E kit — never ship to students
reference/
